name: web-omniroute
version: 1.0.0
description: "OmniRoute web search + content extraction gateway — proxies all search/extract requests through OmniRoute with its provider fallback chains. Requires OMNIROUTE_URL and OMNIROUTE_SEARCH_API_KEY."
author: custom
kind: backend
provides_web_providers:
  - omniroute
